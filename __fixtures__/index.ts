import lights from './lights';
import scene from './scenes';
import effect from './effect';
import states from './states';

export const lightsFixtures = lights;
export const sceneFixtures = scene;
export const effectFixtures = effect;
export const statesFixtures = states;

export default {
    lightsFixtures,
    sceneFixtures,
    effectFixtures
};
